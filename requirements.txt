# VeriMedia backend — core dependencies (all offline-capable, no external
# model downloads required). Tested against Python 3.11.
Flask==3.1.3
numpy==2.4.4
opencv-python-headless==4.13.0.92
Pillow==12.2.0
scikit-learn==1.8.0
scikit-image==0.26.0
scipy==1.17.1
networkx==3.6.1
onnxruntime==1.25.0
pandas==3.0.2
python-dotenv==1.2.2
requests==2.33.1

# Production server + Postgres driver. Lightweight, safe to always install
# (harmless locally with SQLite) and required for deploying behind gunicorn
# with a managed Postgres database — see README "Deploying to DigitalOcean".
gunicorn==23.0.0
psycopg2-binary==2.9.10
